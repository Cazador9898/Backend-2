# Notificaciones Service

Microservicio que recibe eventos internos de ventas mediante `POST /api/notificaciones`. La creación exige el encabezado `X-API-KEY`. Incluye OpenAPI, HATEOAS, pruebas MockMvc y JaCoCo.
